server:
  servlet:
    session:
      cookie:
        name: PHPSESSIONID
      timeout: 600
  port: 8081
spring:

  jpa:
    generate-ddl: false
    show-sql: false
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        query:
          conventional_java_constants: false
  profiles:
    active: dev
